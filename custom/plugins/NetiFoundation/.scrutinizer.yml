build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

      environment:
        php:
          version: 5.6.16

checks:
  php:
    code_rating: true
    duplication: true